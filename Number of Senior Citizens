class Solution(object):
    def countSeniors(self, details):
        """
        :type details: List[str]
        :rtype: int
        """
        count = 0
        for i in details:
            age = int(i[11]+i[12])
            if age > 60:
                count=count+1
        return count
